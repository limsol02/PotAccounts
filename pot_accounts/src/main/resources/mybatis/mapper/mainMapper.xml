<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.pot_accounts.Dao.MainDao">
    <select id="accountsInfo" parameterType="String" resultType="accounts">
        select * from accounts where participant = (select member.member_key from member where id = #{id})
    </select>

    <!--차트 데이터 현재 달-->
    <select id="payMonth01" resultType="payment" parameterType="String">
        SELECT * FROM payment
        WHERE pay_date &gt;= DATE_FORMAT(CURDATE(), '%Y-%m-01')
          AND pay_date &lt; DATE_FORMAT(CURDATE() + INTERVAL 1 MONTH, '%Y-%m-01')
          AND accounts_key = (select member_key from member where id = #{id})
        ORDER BY pay_date
    </select>

    <select id="incomeMonth01" resultType="income" parameterType="String">
        SELECT * FROM income
        WHERE income_date &gt;= DATE_FORMAT(CURDATE(), '%Y-%m-01')
          AND income_date &lt; DATE_FORMAT(CURDATE() + INTERVAL 1 MONTH, '%Y-%m-01')
          AND accounts_key = (select member_key from member where id = #{id})
        ORDER BY income_date
    </select>

    <!--차트 데이터 지난 달-->
    <select id="payMonth02" resultType="payment" parameterType="String">
        SELECT * FROM payment
        WHERE pay_date &gt;= DATE_FORMAT(CURDATE() - INTERVAL 1 MONTH, '%Y-%m-01')
          AND pay_date &lt; DATE_FORMAT(CURDATE(), '%Y-%m-01')
          AND accounts_key = (select member_key from member where id = #{id})
        ORDER BY pay_date
    </select>

    <select id="incomeMonth02" resultType="income" parameterType="String">
        SELECT * FROM income
        WHERE income_date &gt;= DATE_FORMAT(CURDATE() - INTERVAL 1 MONTH, '%Y-%m-01')
          AND income_date &lt; DATE_FORMAT(CURDATE(), '%Y-%m-01')
          AND accounts_key = (select member_key from member where id = #{id})
        ORDER BY income_date
    </select>

    <!--차트 데이터 지지난 달-->
    <select id="payMonth03" resultType="payment" parameterType="String">
        SELECT * FROM payment
        WHERE pay_date &gt;= DATE_FORMAT(CURDATE() - INTERVAL 2 MONTH, '%Y-%m-01')
          AND pay_date &lt; DATE_FORMAT(CURDATE() - INTERVAL 1 MONTH, '%Y-%m-01')
          AND accounts_key = (select member_key from member where id = #{id})
        ORDER BY pay_date
    </select>

    <select id="incomeMonth03" resultType="income" parameterType="String">
        SELECT * FROM income
        WHERE income_date &gt;= DATE_FORMAT(CURDATE() - INTERVAL 2 MONTH, '%Y-%m-01')
          AND income_date &lt; DATE_FORMAT(CURDATE() - INTERVAL 1 MONTH, '%Y-%m-01')
          AND accounts_key = (select member_key from member where id = #{id})
        ORDER BY income_date
    </select>
</mapper>