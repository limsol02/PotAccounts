const BASE_URL = 'http://localhost:9596';
const ACCOUNT = `${BASE_URL}/account`;
const USER = `${BASE_URL}/user`;
const INVITE = `${BASE_URL}/invite`;
const PAYMENT = `${BASE_URL}/payment`;
const INCOME = `${BASE_URL}/income`;
const CALENDAR = `${BASE_URL}/calendar`;
const BUDGET = `${BASE_URL}/budget`;
const CATEGORY = `${BASE_URL}/category`;
const ANALYZE = `${BASE_URL}/analyze`;

const API = {
    BASE_URL: `${BASE_URL}`,
    ACCOUNT: `${ACCOUNT}`,
    USER: `${USER}`,
    INVITE: `${INVITE}`,
    PAYMENT: `${PAYMENT}`,
    INCOME: `${INCOME}`,
    CALENDAR: `${CALENDAR}`,
    BUDGET: `${BUDGET}`,
    CATEGORY: `${CATEGORY}`,
    ANALYZE: `${ANALYZE}`,
}

export default API;