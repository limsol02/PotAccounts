const BASE_URL  = 'http://localhost:9596';
const ACCOUNT  = 'account';
const USER = 'user';
const INVITE = 'invite';
const PAYMENT = "payment"; // 지출
const INCOME = "income"; // 수입
const CALENDAR = 'calendar';
const BUDGET = 'budget';
const CATEGORY = 'category';
const ANALYZE = 'analyze';

const API = {
    BASE_URL  : `${BASE_URL}`,
    ACCOUNT  : `${ACCOUNT}`,
    USER  : `${USER}`,
    INVITE  : `${INVITE}`,
    PAYMENT  : `${PAYMENT}`,
    INCOME  : `${INCOME}`,
    CALENDAR  : `${CALENDAR}`,
    BUDGET  : `${BUDGET}`,
    CATEGORY  : `${CATEGORY}`,
    ANALYZE  : `${ANALYZE}`,
}

export default API;

